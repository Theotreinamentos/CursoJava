package util;

import java.util.Locale;
import java.util.Scanner;

public class IntrProgOrientadaObjetos_CotDollar {
	CurrencyConverter converte = new CurrencyConverter();
	public static void main(String[] args) {
		Locale.setDefault(Locale.US);
		Scanner sc = new Scanner(System.in);
		double totalDollar;
		
		System.out.println("What is the dollar price? ");
		conconverte. = sc.nextDouble();
		
		System.out.println("How many dollars will be bought ? ");
		double qtdeDollar = sc.nextDouble();
		
		System.out.println("What is the dollar price? ");
		double precoDollar = sc.nextDouble();
		
		
		
		
		System.out.println("Voce vai pagar " + totalDollar);
		
		sc.close();

	}

}
